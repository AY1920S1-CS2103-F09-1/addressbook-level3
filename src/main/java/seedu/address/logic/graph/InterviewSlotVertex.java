package seedu.address.logic.graph;

import seedu.address.model.person.Interviewee;

public class InterviewSlotVertex extends Vertex<InterviewSlot, Interviewee> {
    public InterviewSlotVertex(InterviewSlot item) {
        super(item);
    }
}
